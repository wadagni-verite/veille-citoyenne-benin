name: Générer le site HTML

on:
  push:
    paths:
      - 'data/content.json'  # Se déclenche quand vous modifiez les données
      - 'templates/**'
  workflow_dispatch:  # Permet le déclenchement manuel

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Cette ligne donne l'autorisation de "pusher" les modifications
    
    steps:
    - name: Checkout du code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Installer les dépendances
      run: pip install jinja2
      
    - name: Générer le HTML
      run: python scripts/generate-html.py
      
    - name: Vérifier la cohérence (optionnel)
      run: |
        python scripts/validate-data.py
        
    - name: Commit et push du fichier généré
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add output/index.html
        git diff --staged --quiet || (git commit -m "🤖 Mise à jour automatique du site - $(date)" && git push)

